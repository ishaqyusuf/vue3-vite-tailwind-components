const mounted = () => {
  // Do something in mounted()
};
const updated = () => {
  // Do something in updated()
};

const unmounted = () => {
  // Do something in unmounted()
};

export default {
  mounted,
  updated,
  unmounted,
};
